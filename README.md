The code is written in C++ and uses BFS(Breadth First Search) Algorithm to explore the nodes. 
The algorithm adds the neighbors of a node to the queue and loop on nodes taking a node in queue concept First In First Out. 
the start node is labeled A, the goal B and walls are # symbols. 
After solving the maze we print the trace from the start to the goal in * symbols.
